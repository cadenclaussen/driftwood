Build and launch the Xcode project in the iPhone 16 simulator.

Steps:
1. Auto-detect the .xcworkspace or .xcodeproj file in the current directory
2. Use the first available scheme if multiple exist
3. Build for Debug configuration
4. Launch in iPhone 16 simulator with latest iOS version
5. Use xcodebuild with --quiet flag to minimize output
6. Report build status and any errors
